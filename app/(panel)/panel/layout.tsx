import "./admin.css";

/**
 * Root panel layout — shared CSS only.
 * Authenticated shell lives in (app)/layout.tsx
 */
export default function PanelRootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
